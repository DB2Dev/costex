from dataclasses import dataclass

# from typing import List
# from enum import Enum, auto


@dataclass
class Cost:
    pass
    # table_name: str
    # table_type: str
    # cost: float
    # total_records: int
    # primary_key: str
    # columns: dict
    # # columns: List[Column]

    # def __post_init__(self):
    #     self.columns = {column.name: column for column in self.columns}

    # def __call__(self):
    #     return self.cost
    #     # return estimate_select(self)
